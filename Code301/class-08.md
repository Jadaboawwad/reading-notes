## 1.

        in this part we will discuss the following points

* What does REST stand for?
* REST APIs are designed around a ____.
* What is an identifer of a resource? Give an example.
* What are the most common HTTP verbs?
* What should the URIs be based on?
* Give an example of a good URI.
* What does it mean to have a ‘chatty’ web API? Is this a good or a bad thing?
* What status code does a successful GET request return?
* What status code does an unsuccessful GET request return?
* What status code does a successful POST request return?
* What status code does a successful DELETE request return?

<br/>

                           The beginning of Part One

### 1.1: What does REST stand for? 

Representational State Transfer 

### 1.2: REST APIs are designed around a ____. 

Resources

### 1.3: What is an identifer of a resource? Give an example. 

which is a URI that uniquely identifies that resource. For example, the URI for a particular customer order might be:

```html
https://adventure-works.com/orders/1
```

### 1.4: What are the most common HTTP verbs?

 GET, POST, PUT, PATCH, and DELETE.

### 1.5: What should the URIs be based on?

 based on nouns

### 1.6: Give an example of a good URI.

```url
https://adventure-works.com/orders // Good
```

### 1.7: What does it mean to have a ‘chatty’ web API? Is this a good or a bad thing?

 that expose a large number of small resources.

### 1.8: What status code does a successful GET request return?

s HTTP status code 200 (OK)

### 1.9: What status code does an unsuccessful GET request return?

return 404 (Not Found).

### 1.10: What status code does a successful POST request return?

Return HTTP status code 202 (Accepted) 

### 1.11: What status code does a successful DELETE request return?

303
<br/>

    
                               The End of Part One
                               

# [BACK TO HOME](https://jehadabuawwad.github.io/reading-notes)
